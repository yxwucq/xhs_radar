# 本地模型蒸馏方案讨论

## 背景

当前项目（XHS Content Radar）使用 API 调用大模型对小红书笔记进行低质量内容分类。考虑蒸馏出一个本地小模型替代 API，以提升速度、降低成本、支持离线使用。

## 当前分类数据现状

项目从小红书信息流卡片 DOM 中提取以下字段：

| 字段 | 说明 | 实际情况 |
|------|------|---------|
| title | 笔记标题 | 主要分类信号，始终存在 |
| author | 作者名 | 辅助上下文 |
| likeCount | 点赞数（字符串） | 互动量参考 |
| content | 笔记正文预览 | 信息流卡片中几乎永远为空 |

**结论：分类主要依赖标题，信息量有限。**

## 模型方案对比

### 加载速度与体积

| 方案 | 模型大小 | 首次加载（含下载） | 二次加载（本地缓存） | 单条推理 |
|------|---------|-------------------|---------------------|---------|
| API 调用（现状） | 无 | 即时 | 即时 | 300-2000ms（网络往返） |
| TextCNN | ~5-10MB | ~300-600ms | ~50-100ms | ~2-5ms |
| **MiniLM INT8（推荐）** | ~25-50MB | ~5-10s | **~500ms-1s** | **~15-30ms** |
| DistilBERT | ~200-250MB | ~15-30s | ~2-3s | ~50-100ms |

- 首次加载：安装扩展后第一次使用，需从 CDN 下载模型文件
- 二次加载：模型已缓存在 IndexedDB，后续打开页面只需本地读取和初始化

### 本地算力要求（MiniLM INT8）

| 资源 | 占用 |
|------|------|
| 内存 | ~50-100MB（模型常驻） |
| CPU | 单核即可，单次推理 ~15-30ms |
| GPU | 不需要，有 WebGPU 可加速到 ~5-10ms |
| 磁盘 | ~30-50MB 缓存 |

**任何能跑 Chrome 的电脑都能流畅运行。**

### 训练资源需求（MiniLM INT8）

| 项目 | 估算 |
|------|------|
| 训练数据量 | 5000-10000 条 |
| 显存 | ~2-4GB（无 GPU 用 CPU 也行） |
| 训练时间（GPU） | ~10-30 分钟（3060 级别） |
| 训练时间（CPU） | ~2-4 小时（M系列 Mac 可行） |

完整流程：大模型标注数据 → 数据清洗 → 微调 MiniLM → INT8 量化 → ONNX 导出，Mac 本地即可完成。

## 核心问题：无法自定义细分分类

蒸馏模型的分类体系在训练时就固定了。用户想新增过滤类别（如"AI生成内容"），需要重新收集数据、重新训练、重新发布。而 API 方案只需改 prompt。

### 折中方案：本地粗筛 + API 细分

- 本地模型负责二分类：**是否低质量**（0-100 分数）
- 用户自定义的细分标签、个性化规则走 API prompt 处理
- 高置信度内容本地直接处理，边界 case 回退 API 复核
- 兼顾速度、成本和灵活性

## 后续可优化方向

- 获取更多输入特征（笔记详情页正文、图片 OCR、标签）提升分类精度
- 首次冷启动加进度条过渡体验
- 模型版本管理与增量更新机制
